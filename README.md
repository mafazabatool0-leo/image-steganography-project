This program performs image steganography using Python and OpenCV.

First, the user uploads two images:
- A cover image (main image)
- A secret image (image to hide)

Both images are read using OpenCV. The secret image is resized to match the dimensions of the cover image so that pixel-wise operations can be applied correctly.
